"""API surface of the Project ATLAS backend (IP-001 §6 — ``backend/api/``).

Owns the operational (health/metrics) endpoints and the versioned API
router registration. Contains presentation-layer wiring only — no
business logic (RA-001 §5, ES-001 §3).
"""
